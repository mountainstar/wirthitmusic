/**
 * Curated media for /profile — edit these lists with your own links.
 *
 * SoundCloud: full track URLs (open a track → Share → Copy link), e.g. https://soundcloud.com/user/track-name
 * (Your profile: https://soundcloud.com/wirth_it — pick individual tracks, not the profile URL.)
 * YouTube: used when API env vars are not set (see .env.example for channel/playlist loading).
 */
export const PROFILE_SOUNDCLOUD_TRACK_URLS: string[] = [
  // "https://soundcloud.com/wirth_it/example-track",
];

export const PROFILE_YOUTUBE_VIDEO_IDS: string[] = [
  // "dQw4w9WgXcQ",
];
